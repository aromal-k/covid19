import { environment } from '../../../../environments/environment';
export const UrlConstants = {
  covidCasesURL: `${environment.baseURL}v2/all`,
  countriesURL: `${environment.baseURL}v2/countries`
};
